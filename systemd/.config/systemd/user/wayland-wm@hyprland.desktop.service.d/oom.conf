# Apps launched from Hyprland share its cgroup, so the default OOMPolicy=stop
# tears down the whole session (and every terminal) when the kernel OOM-kills
# one of them, e.g. a browser tab. Let the OOM victim die alone instead.
[Service]
OOMPolicy=continue
